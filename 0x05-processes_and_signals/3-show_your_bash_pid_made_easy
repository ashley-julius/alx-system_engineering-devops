#!/usr/bin/env bash
# Find the PIDs of processes containing the word "bash" in their name
pids=$(pgrep bash)

# Iterate through each PID and display its PID and name
for pid in $pids; do
  process_name=$(ps -p "$pid" -o comm=)
  echo "PID: $pid, Process Name: $process_name"
done
